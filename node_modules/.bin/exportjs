#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../export/bin/exportjs" "$@"
  ret=$?
else 
  node  "$basedir/../export/bin/exportjs" "$@"
  ret=$?
fi
exit $ret
