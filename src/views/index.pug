doctype html
html(lang="en", ng-app="tg", ng-controller="AppCtrl")
    head
        meta(charset='utf-8')
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        meta(name="description" content="")
        title trendGit
        link(rel='icon', type='image/x-icon', href='/img/favicon.ico')
        link(rel="stylesheet" type="text/css" href="/less/index.css")
        script(src="https://use.fontawesome.com/b3e6544295.js")
    body
        div(header, class="{{loggedIn ? '' : 'logged-out'}} {{collapsed}}")
        div.left-col(ng-if="loggedIn")
            div(nearby)
        div(feed, class="{{loggedIn ? '' : 'logged-out'}}")
        div.right-col
            div(topusers)
            div(footer)
        script(src="/js/app.js")
        script.
            window.onbeforeunload = function() {window.scrollTo(0,0);}
